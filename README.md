# 6thtool

This project was exported from Replit.

## Notes on This Export

### Excluded Files
28 large video/media files (35MB–168MB) were excluded from this export due to GitHub's 100MB file size limit. These include `.mp4` and `.mov` files in the `attached_assets/` and `client/public/assets/` directories. They remain in the original Replit project.

### Git History
This repository contains a single export commit rather than the full development history. The original git history is preserved in Replit and contains ~1.5GB of binary video assets that cannot be hosted on GitHub.

### Future Pushes
To push updates from Replit, run:
```bash
git push origin main
```
The `GITHUB_PERSONAL_ACCESS_TOKEN` secret is configured in Replit for authentication.
